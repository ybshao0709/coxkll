% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coxkl_highdim.R
\name{coxkl_highdim}
\alias{coxkl_highdim}
\title{Cox Proportional Hazards Model with KL Divergence for High-Dimensional Data}
\usage{
coxkl_highdim(
  z,
  delta,
  time,
  RS = NULL,
  beta = NULL,
  eta_list = 0,
  alpha = 1,
  nlambda = 100,
  lambda,
  lambda.min = 0.001,
  eps = 0.001,
  max.iter = 1000,
  dfmax = p,
  warn = TRUE,
  returnX = FALSE,
  activeSet = FALSE,
  actIter = 50,
  actNum = 5,
  ...
)
}
\arguments{
\item{z}{A matrix of covariates. Rows represent observations, and columns
represent variables.}

\item{delta}{A vector of event indicators (1 = event, 0 = censored).}

\item{time}{A vector of survival/censoring times.}

\item{RS}{Numeric vector or matrix of precomputed external risk scores.
Length (or number of rows) must match the number of observations.
If not provided, \code{beta} must be specified.}

\item{eta_list}{A \emph{single} numeric value controlling the influence of the
external risk score (\code{RS}). A higher \code{eta_list} gives more weight
to the external information. Default is 0.}

\item{alpha}{The elastic net mixing parameter.  \code{alpha = 1} corresponds to
lasso, \code{alpha = 0} corresponds to ridge, and values between 0 and 1
represent a mixture of the two.  Default is 1 (lasso).}

\item{nlambda}{The number of lambda values to use in the regularization path.
Default is 100.  The function automatically generates a sequence of lambda
values.}

\item{lambda}{An optional user-specified sequence of lambda values.  If
provided, \code{nlambda} is ignored.  It's generally recommended to let the
function generate the lambda sequence unless you have specific requirements.}

\item{lambda.min}{The ratio of the smallest lambda value to the largest lambda
value in the automatically generated sequence.  Default is 0.001.}

\item{eps}{Convergence threshold for the coordinate descent algorithm.
Default is 0.001.}

\item{max.iter}{The maximum number of iterations for the coordinate descent
algorithm. Default is 1000.}

\item{dfmax}{Maximum number of non-zero coefficients.}

\item{warn}{Logical value indicating whether to print warnings. Default is
TRUE.}

\item{returnX}{Logical value. If TRUE, the design matrix \code{z} is included
in the returned list.}

\item{activeSet}{Logical. If TRUE, an active set strategy is used to speed up computation.}

\item{actIter}{Integer. Maximum number of iteration in active set cycling.}

\item{actNum}{Integer. Maximum number of variables added to the active set at each step.}

\item{...}{Additional arguments (not used in this simplified documentation,
but potentially used by the underlying implementation).}
}
\value{
A list containing:
\item{beta}{A matrix of coefficients.  Each \emph{column} corresponds to a
different value of \code{lambda} in the regularization path.  The number of
rows is equal to the number of covariates (number of columns in \code{z}).}
\item{lambda}{The sequence of \code{lambda} values used.}
\item{df}{The number of non-zero coefficients for each value of \code{lambda}.}
\item{iter}{The number of iterations taken for each value of \code{lambda}.}
\item{group}{The \code{group} vector provided as input.}
\item{alpha}{The \code{alpha} provided as input.}
\item{X}{If \code{returnX = TRUE}, the design matrix \code{z} is returned.}
}
\description{
This function estimates the coefficients of a Cox proportional hazards model
using Kullback-Leibler (KL) divergence for data integration in high-dimensional
settings. It incorporates an external risk score and supports regularization via lasso or ridge penalties.
It is designed for situations where the number of covariates (p) can be larger than the number
of observations (n).
}
\examples{
data("ExampleDataHighDim")
rs <- as.matrix(ExampleData$z) \%*\% as.matrix(ExampleData$beta_external) # this can be any risk score
result2 <- coxkl_highdim(z = ExampleData$z, delta = ExampleData$status,
                         time = ExampleData$time,
                         RS = rs,
                         group = c(1:dim(ExampleData$z)[2]), eta_list = c(1),
                         cv.method = "LinPred")

}
