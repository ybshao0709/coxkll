% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.coxkl}
\alias{predict.coxkl}
\title{Predict method for objects of class "coxkl"}
\usage{
\method{predict}{coxkl}(object, newz, delta = NULL, time = NULL, likelihood = TRUE, ...)
}
\arguments{
\item{object}{An object of class \code{"coxkl"} returned by
\code{\link{coxkl}}.}

\item{newz}{Numeric matrix (n_new × p) of covariates for which
predictions are required.  It must have the same
number and ordering of columns used in training.}

\item{delta}{Numeric event indicator vector (1 = event, 0 = censored)
for the new data.  Required if \code{likelihood = TRUE}.}

\item{time}{Numeric vector of event or censoring times for the
new data.  Required if \code{likelihood = TRUE}.}

\item{likelihood}{Logical; if \code{TRUE} (default) the log partial
likelihood is evaluated on the new data with
\code{\link{pl_cal_theta}}.  Set to \code{FALSE} if
you only need the linear predictors.}

\item{...}{Currently ignored; included for S3 compatibility.}
}
\value{
A list with components
\describe{
\item{\code{LP_list}}{List of length \code{length(object$eta_list)}
containing the linear predictors for each \eqn{\eta}.}
\item{\code{beta_list}}{The coefficient vectors carried over from
the fitted object.}
\item{\code{eta_list}}{The \eqn{\eta} grid used in fitting.}
\item{\code{likelihood}}{Numeric vector of log partial likelihood
values on the new data (returned only if
\code{likelihood = TRUE}).}
}
}
\description{
Computes linear predictors (and, if desired, the log partial
likelihood) for new observations given a fitted Cox–KL model.
}
\examples{
fit <- coxkl(z      = train$z,
             delta  = train$delta,
             time   = train$time,
             beta   = beta_external,
             eta_list = seq(0, 5, by = 1))

pred <- predict(fit,
                newz  = test$z,
                delta = test$delta,
                time  = test$time)

# Linear predictors for eta = 2
head(pred$LP_list[[which(fit$eta_list == 2)]])

# Corresponding log partial likelihood on the test set
pred$likelihood[which(fit$eta_list == 2)]
}
